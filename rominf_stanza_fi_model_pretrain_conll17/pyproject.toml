[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "rominf-stanza-fi-model-pretrain-conll17"
version = "1.8.0"

[tool.setuptools.packages.find]
where = ["data"]
